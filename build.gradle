buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.0.0'
        classpath files('gradle/libs/gradle-witness.jar')
    }
}

allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}
